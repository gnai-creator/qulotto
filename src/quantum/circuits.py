class QuantumCircuitBuilder:
    """Placeholder para construcao de circuitos quanticos do projeto."""

    def build(self, *args, **kwargs):
        raise NotImplementedError(
            "QuantumCircuitBuilder ainda nao foi implementado."
        )
